#include <iostream>
#include "ros/ros.h"
#include "listener.h"
using namespace std;

int main(int argc,char **argv)
{
    ROS_INFO("start to listen...");

    ros::init(argc, argv, "listen");
    listener lt;

    ros::spinOnce();
    return 0;
}
